// =============================================
// YMM Africa — Main JS
// Nav, carousel, flip cards, form, scroll reveal
// =============================================

document.addEventListener('DOMContentLoaded', () => {
  // Nav
  // Carousel
  // Flip cards
  // Form validation
  // Scroll reveal
});
